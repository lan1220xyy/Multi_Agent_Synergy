import { PagePlaceholder } from "@/components/page-placeholder";

export default function AdminSystemPage() {
  return (
    <PagePlaceholder
      title="系统健康"
      description="事件循环延迟 + 并发数 + failed Teams + idle 告警"
    />
  );
}
