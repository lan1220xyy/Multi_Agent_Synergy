import { PagePlaceholder } from "@/components/page-placeholder";

export default function MetricsPage() {
  return (
    <PagePlaceholder
      title="指标"
      description="KPI 卡片 + Team 状态分布 + Agent 性能 + A2A 流量"
    />
  );
}
