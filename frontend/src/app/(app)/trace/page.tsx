import { PagePlaceholder } from "@/components/page-placeholder";

export default function TracePage() {
  return (
    <PagePlaceholder
      title="Trace Explorer"
      description="按 trace_id 检索，span 树状图 + 跳转 Jaeger"
    />
  );
}
