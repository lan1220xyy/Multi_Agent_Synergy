import { PagePlaceholder } from "@/components/page-placeholder";

export default function ProjectsPage() {
  return (
    <PagePlaceholder
      title="项目导入"
      description="CSV 拖拽 + 预览 + 校验，触发自动建 Team"
    />
  );
}
